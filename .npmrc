# Hoist all packages to root node_modules so that transitive dependencies of
# bundled workspace packages (e.g. winston, pg, drizzle-orm) are reachable by
# the compiled dist/index.js at runtime on Render (or any other host).
shamefully-hoist=true
